<view class="container">
  <view wx:if="{{loading}}" class="loading">
    <text>加载中...</text>
  </view>

  <view wx:else>
    <view wx:if="{{suppliers.length === 0}}" class="empty-state">
      <text class="empty-icon">🏭</text>
      <text>暂无供应商</text>
    </view>

    <view wx:else class="supplier-list">
      <view
        class="supplier-card"
        wx:for="{{suppliers}}"
        wx:key="id"
      >
        <view class="supplier-header">
          <text class="supplier-name">{{item.name}}</text>
        </view>
        <view class="supplier-body">
          <view class="supplier-row">
            <text class="label">联系人:</text>
            <text class="value">{{item.contact}}</text>
          </view>
          <view class="supplier-row">
            <text class="label">电话:</text>
            <text class="value">{{item.phone}}</text>
          </view>
          <view wx:if="{{item.address}}" class="supplier-row">
            <text class="label">地址:</text>
            <text class="value">{{item.address}}</text>
          </view>
        </view>
        <view class="supplier-actions">
          <button class="action-btn edit" bindtap="navigateToEdit" data-id="{{item.id}}">编辑</button>
          <button class="action-btn delete" bindtap="deleteSupplier" data-id="{{item.id}}">删除</button>
        </view>
      </view>
    </view>
  </view>

  <view class="fab" bindtap="navigateToAdd">+</view>
</view>

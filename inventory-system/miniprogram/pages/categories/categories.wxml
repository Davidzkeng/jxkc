<view class="container">
  <view wx:if="{{loading}}" class="loading">
    <text>加载中...</text>
  </view>

  <view wx:else>
    <view wx:if="{{categories.length === 0}}" class="empty-state">
      <text class="empty-icon">📋</text>
      <text>暂无类别</text>
    </view>

    <view wx:else class="category-list">
      <view
        class="category-card"
        wx:for="{{categories}}"
        wx:key="id"
      >
        <view class="category-header">
          <view class="category-info">
            <text class="category-name">{{item.name}}</text>
            <text wx:if="{{item.description}}" class="category-desc">{{item.description}}</text>
          </view>
        </view>
        <view class="category-actions">
          <button class="action-btn edit" bindtap="navigateToEdit" data-id="{{item.id}}">编辑</button>
          <button class="action-btn delete" bindtap="deleteCategory" data-id="{{item.id}}">删除</button>
        </view>
      </view>
    </view>
  </view>

  <view class="fab" bindtap="navigateToAdd">+</view>
</view>
